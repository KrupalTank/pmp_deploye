@model PlacementMentorshipPortal.Models.Branch

@{
    ViewData["Title"] = "Edit Branch";
}

<link rel="stylesheet" href="~/css/forms.css" />

<h2 class="page-title">🏫 Edit Branch</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success text-center">
        @TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger text-center">
        @TempData["Error"]
    </div>
}

<div class="form-card">

    <form asp-action="EditBranchMethod">

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <input type="hidden" asp-for="Bid" />

        <!-- Branch Name -->
        <div class="mb-3">
            <label asp-for="Bname" class="form-label">Branch Name</label>
            <input asp-for="Bname" class="form-control" />
            <span asp-validation-for="Bname" class="text-danger"></span>
        </div>

        <!-- Submit -->
        <button type="submit" class="btn btn-primary btn-submit">
            💾 Save Changes
        </button>
    </form>

    <!-- Back -->
    <a asp-controller="Admin" asp-action="Profile" class="back-link text-primary">
        ← Go to Profile Page
    </a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
