@model PlacementMentorshipPortal.Models.Description

@{
    ViewData["Title"] = "Edit Description";
}

<link rel="stylesheet" href="~/css/forms.css" />

<h2 class="page-title">✏️ Edit Description</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success text-center">
        @TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger text-center">
        @TempData["Error"]
    </div>
}

<div class="form-card">

    <form asp-action="EditDescriptionMethod">

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <input type="hidden" asp-for="Id" />

        <!-- Coordinator -->
        <div class="mb-3">
            <label class="form-label">Coordinator</label>
            <input type="text" class="form-control" value="@User.Identity.Name" readonly />
        </div>

        <!-- Company Dropdown -->
        <div class="mb-3">
            <label asp-for="Cid" class="form-label">Company</label>
            <select asp-for="Cid" class="form-control" asp-items="ViewBag.Cid"></select>
            <span asp-validation-for="Cid" class="text-danger"></span>
        </div>

        <!-- Description -->
        <div class="mb-3">
            <label asp-for="Dtext" class="form-label">Description</label>
            <textarea asp-for="Dtext" rows="5" class="form-control"></textarea>
            <span asp-validation-for="Dtext" class="text-danger"></span>
        </div>

        <!-- Created Date -->
        <div class="mb-3">
            <label asp-for="Createdat" class="form-label">Created Date</label>
            <input asp-for="Createdat" type="date" class="form-control" />
            <span asp-validation-for="Createdat" class="text-danger"></span>
        </div>

        <!-- Submit -->
        <button type="submit" class="btn btn-primary btn-submit">
            💾 Save Changes
        </button>
    </form>

    <!-- Back -->
    <a asp-action="Index" class="back-link text-primary">
        ← Back to List
    </a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
