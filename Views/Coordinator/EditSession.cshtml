@model PlacementMentorshipPortal.Models.Session

@{
    ViewData["Title"] = "Edit Session";
}

<link rel="stylesheet" href="~/css/forms.css" />

<h2 class="page-title">🗓️ Edit Session</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success text-center">
        @TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger text-center">
        @TempData["Error"]
    </div>
}

<div class="form-card">

    <form asp-action="EditSessionMethod">

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <input type="hidden" asp-for="Id" />

        <!-- Coordinator -->
        <div class="mb-3">
            <label class="form-label">Coordinator</label>
            <input type="text" class="form-control" value="@User.Identity.Name" readonly />
        </div>

        <!-- Branch Dropdown -->
        <div class="mb-3">
            <label asp-for="Bid" class="form-label">Branch</label>
            <select asp-for="Bid" class="form-control" asp-items="ViewBag.Bid"></select>
            <span asp-validation-for="Bid" class="text-danger"></span>
        </div>

        <!-- Session Detail -->
        <div class="mb-3">
            <label asp-for="Detail" class="form-label">Session Detail</label>
            <input asp-for="Detail" class="form-control" />
            <span asp-validation-for="Detail" class="text-danger"></span>
        </div>

        <!-- Session Time -->
        <div class="mb-3">
            <label asp-for="Time" class="form-label">Session Time</label>
            <input asp-for="Time" class="form-control" />
            <span asp-validation-for="Time" class="text-danger"></span>
        </div>

        <!-- Submit -->
        <button type="submit" class="btn btn-primary btn-submit">
            💾 Save Changes
        </button>
    </form>

    <!-- Back -->
    <a asp-action="Index" class="back-link text-primary">
        ← Back to List
    </a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
